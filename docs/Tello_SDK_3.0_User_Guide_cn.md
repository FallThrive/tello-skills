# ROBOMASTER TT SDK 3.0 使用说明

**版本：** 2021.04 V1.0  
**版权所有：** ©2021 睿炽科技

---

## 目录

1. [概述](#1-概述)
2. [安全功能](#2-安全功能)
3. [重置 Wi-Fi](#3-重置-wi-fi)
4. [架构](#4-架构)
5. [发送命令和接收响应](#5-发送命令和接收响应)
6. [接收 Tello 状态](#6-接收-tello-状态)
7. [接收 Tello 视频流](#7-接收-tello-视频流)
8. [Tello 命令类型和结果](#8-tello-命令类型和结果)
9. [UDP → Tello 命令](#9-udp--tello-命令)
   - [控制命令](#91-控制命令)
   - [设置命令](#92-设置命令)
   - [读取命令](#93-读取命令)
10. [开源控制器 → Tello 命令](#10-开源控制器--tello-命令)
11. [UDP → 开源控制器 ESP32](#11-udp--开源控制器-esp32)
12. [Tello 状态详解](#12-tello-状态详解)
13. [Mid 命令限制](#13-mid-命令限制)
14. [附录：注释说明](#14-附录注释说明)

---

## 1. 概述

TT SDK 通过 Wi-Fi UDP 协议与飞行器连接，让用户可以通过文本指令控制飞行器。

用户安装 RoboMaster SDK 对 TT 进行控制，包括控制 TT 的开源控制器以及飞行器。具体使用方法请参考如下网址：

[https://robomaster-dev.readthedocs.io/zh_CN/latest/](https://robomaster-dev.readthedocs.io/zh_CN/latest/)

---

## 2. 安全功能

如果 Tello 在执行完当前指令后，**15 秒内**没有收到任何命令输入（开源控制器发送 `[TELLO]battery?` 查询电量指令除外），将**自动降落**。

---

## 3. 重置 Wi-Fi

在开机状态下，长按电源键 5 秒，期间状态指示灯熄灭后飞行器将重启。状态指示灯显示黄灯快闪后，Wi-Fi 的 SSID 和密码将重置为出厂设置，默认无密码。

---

## 4. 架构

建立 Tello 和 PC、Mac 或移动设备之间的 Wi-Fi 通信。

---

## 5. 发送命令和接收响应

| 项目 | 详情 |
|:---|:---|
| **Tello IP** | 192.168.10.1 |
| **UDP PORT** | 8889 |
| **通信方向** | PC / Mac / Mobile ⟷ Tello |

**备注 1：** 在 PC、Mac 或移动设备上设置 UDP 客户端，向 Tello UDP 端口 8889 发送命令和接收响应。

**备注 2：** 在发送所有其他命令之前，向 Tello UDP 端口 8889 发送 `command` 命令以启动 Tello 的 SDK 模式。

---

## 6. 接收 Tello 状态

| 项目 | 详情 |
|:---|:---|
| **Tello IP** | 192.168.10.1 |
| **目标 IP** | 0.0.0.0 |
| **UDP PORT** | 8890 |
| **通信方向** | Tello → PC / Mac / Mobile |

**备注 3：** 先完成备注 1 和 2 的操作。在 PC、Mac 或移动设备上建立 UDP 服务器，通过 UDP 端口 8890 从 IP 0.0.0.0 收听消息。

> 详细状态信息请查看第 12 节「Tello 状态详解」。

---

## 7. 接收 Tello 视频流

| 项目 | 详情 |
|:---|:---|
| **Tello IP** | 192.168.10.1 |
| **目标 IP** | 0.0.0.0 |
| **UDP PORT** | 11111 |
| **通信方向** | Tello → PC / Mac / Mobile |

**备注 4：** 在 PC、Mac 或移动设备上设置 UDP 服务器，通过服务器 UDP 端口 11111 从 IP 0.0.0.0 收听消息。

**备注 5：** 先进行备注 1 和 2 的操作，然后向 Tello UDP 端口 8889 发送 `streamon` 命令，开始接收 Tello 视频流。

---

## 8. Tello 命令类型和结果

Tello 接收的 SDK 命令包含三种基本命令类型：

| 命令类型 | 格式 | 说明 |
|:---|:---|:---|
| **控制命令** | `xxx` | 如果命令执行成功，则返回 `ok`。如果不成功，返回 `error` 或信息结果代码。 |
| **设置命令** | `xx a` | 设置命令将尝试设置新的子参数值 `a`。如果命令执行成功，则返回 `ok`。如果不成功，返回 `error` 或信息结果代码。 |
| **读取命令** | `xx?` | 读取实时子参数值。 |

---

## 9. UDP → Tello 命令

### 9.1 控制命令

| 命令 | 描述 | 参数范围 | 可能的响应 |
|:---|:---|:---|:---|
| `command` | 进入 SDK 命令模式 | — | `ok` / `error` / `unactive` |
| `takeoff` | 自动起飞 | — | `ok` / `error` |
| `land` | 自动降落 | — | `ok` / `error` |
| `streamon` | 打开视频流 | — | `ok` / `error` |
| `streamoff` | 关闭视频流 | — | `ok` / `error` |
| `emergency` | 停止电机转动 | — | `ok` / `error` |
| `up x` | 向上飞 x 厘米 | `x = 20~500` | `ok` / `error` + 错误状态 |
| `down x` | 向下飞 x 厘米 | `x = 20~500` | `ok` / `error` + 错误状态 |
| `left x` | 向左飞 x 厘米 | `x = 20~500` | `ok` / `error` + 错误状态 |
| `right x` | 向右飞 x 厘米 | `x = 20~500` | `ok` / `error` + 错误状态 |
| `forward x` | 向前飞 x 厘米 | `x = 20~500` | `ok` / `error` + 错误状态 |
| `back x` | 向后飞 x 厘米 | `x = 20~500` | `ok` / `error` + 错误状态 |
| `cw x` | 顺时针旋转 x° | `x = 1~360` | `ok` / `error` |
| `ccw x` | 逆时针旋转 x° | `x = 1~360` | `ok` / `error` |
| `motoron` | 进入起桨模式（*注 1） | — | `ok` / `error` |
| `motoroff` | 退出起桨模式 | — | `ok` / `error` |
| `throwfly` | 抛飞，5 秒以内将飞机水平抛出，即可完成抛飞 | — | `ok` / `error` |
| `flip x` | 朝 x 方向翻滚 | `l` = left（左）<<br>`r` = right（右）<<br>`f` = forward（前）<<br>`b` = back（后） | `ok` / `error` + 错误状态 |
| `go x y z speed` | 以设置速度（cm/s）飞往坐标 (x,y,z) | `x, y, z: -500 ~ 500`<br>`speed: 10~100`（cm/s）<<br>x、y、z 不能同时在 -20 ~ 20 之间 | `ok` / `error` + 错误状态 |
| `stop` | 停止运动并悬停，任何时候都可以 | — | `ok` / `error` |
| `curve x1 y1 z1 x2 y2 z2 speed` | 以设置速度（cm/s）飞弧线，经过 (x1,y1,z1) 到 (x2,y2,z2) | `x1, x2: -500 ~ 500`<br>`y1, y2: -500 ~ 500`<br>`z1, z2: -500 ~ 500`<br>`speed: 10~60`<br>x、y、z 不能同时在 -20 ~ 20 之间<br>如果弧线半径不在 0.5~10 米范围内，则返回相应提醒。 | `ok` / `error` + 错误状态 |
| `go x y z speed mid` | 以设置速度（cm/s）飞往设置 id 的挑战卡坐标系的 (x,y,z) 坐标点（*注 2） | `x, y: -500 ~ 500`<br>`z: 0 ~ 500`<br>`speed: 10~100`（cm/s）<<br>x、y、z 不能同时在 -20 ~ 20 之间 | `ok` / `error` + 错误状态 |
| `curve x1 y1 z1 x2 y2 z2 speed mid` | 以设置速度（cm/s）飞弧线，经过设置 mid 的挑战卡坐标系中的 (x1,y1,z1) 点到 (x2,y2,z2) 点 | `x1, x2: -500 ~ 500`<br>`y1, y2: -500 ~ 500`<br>`z1, z2: 0 ~ 500`<br>`speed: 10~60`<br>x、y、z 不能同时在 -20 ~ 20 之间<br>如果弧线半径不在 0.5~10 米范围内，则返回相应提醒。 | `ok` / `error` + 错误状态 |
| `jump x y z speed yaw mid1 mid2` | Tello 飞往 mid1 坐标系的 (x,y,z) 点后悬停，识别 mid2 的挑战卡，并在 mid2 坐标系下 (0,0,z) 的位置并旋转到设置的 yaw 值 | `z > 0` | `ok` / `error` + 错误状态 |
| `reboot` | 重启飞行器 | — | 无响应（成功）/ `error` |

### 9.2 设置命令

| 命令 | 描述 | 参数范围 | 可能的响应 |
|:---|:---|:---|:---|
| `speed x` | 将当前速度设为 x cm/s | `x = 10~100` | `ok` / `error` |
| `rc a b c d` | 设置遥控器的 4 个通道杆量 | `a`（横滚）: -100~100<br>`b`（俯仰）: -100~100<br>`c`（油门）: -100~100<br>`d`（偏航）: -100~100 | 无响应 |
| `wifi ssid pass` | 更改 Tello Wi-Fi 密码 | `ssid`: 更改后的 Wi-Fi 账号<br>`pass`: 更改后的 Wi-Fi 密码<br>如接入开源控制器，ssid 默认会加上 `RMTT-` 前缀；若未接入则加上 `TELLO-` 前缀 | `OK, drone will reboot in 3s` |
| `mon` | 打开挑战卡探测 | 默认打开下视探测 | `ok` / `error` |
| `moff` | 关闭挑战卡探测 | — | `ok` / `error` |
| `mdirection x` | 设置探测方向 | `x = 0/1/2`<br>`0` = 打开下视探测<br>`1` = 打开前视探测<br>`2` = 同时打开前视和下视探测<br><br>* 使用前必须使用 `mon` 命令打开探测功能，默认打开下视探测；<<br>* 单独打开前视或者下视探测时，探测频率为 20Hz；同时打开前视和下视时，将交替探测，单个方向的探测频率为 10Hz。 | `ok` / `error` |
| `ap ssid pass` | 将 Tello 转为 station 模式，并连入到 AP | `ssid`: 要连接的 Wi-Fi 账号<br>`pass`: 要连接的 Wi-Fi 密码 | `OK, drone will reboot in 3s` |
| `wifisetchannel xxx` | 设置开源控制器 Wi-Fi 信道 | `xxx` 为需要设置的信道。<br>注意：清除信道设置需要清除 WiFi 信息，请根据各个地区政策设置符合法规的信道<br>（仅开源控制器支持） | `ok` / `error` |
| `port info video` | 设置状态信息和视频流推送端口 | `info`: 状态信息推送端口<br>`video`: 视频信息推送端口<br>范围: 1025~65535 | `ok` / `error` |
| `setfps fps` | 设置视频流帧率 | `fps`: `high`（30fps）、`middle`（15fps）、`low`（5fps） | `ok` / `error` |
| `setbitrate bitrate` | 设置视频流比特率 | `bitrate`: 0~5，分别代表自动、1Mbps、2Mbps、3Mbps、4Mbps、5Mbps | `ok` / `error` |
| `setresolution resolution` | 设置视频流分辨率 | `resolution`: `high`（720P）、`low`（480P） | `ok` / `error` |

### 9.3 读取命令

| 命令 | 描述 | 返回值说明 | 可能的响应 |
|:---|:---|:---|:---|
| `speed?` | 获取当前设置速度 | `x = 10~100`（cm/s） | `x` |
| `battery?` | 获取当前电池剩余电量的百分比值 | `x = 10~100`（%） | `x` |
| `time?` | 获取电机运转时间 | 单位：秒 | `x` |
| `wifi?` | 获得 Wi-Fi 信噪比 | — | 信噪比值 |
| `sdk?` | 获得 Tello SDK 版本号 | `xx`（>=20） | `xx` |
| `sn?` | 获得 Tello SN 码 | — | 生产序列号 |
| `hardware?` | 获得 TT 是否接入开源控制器 | 接入返回 `RMTT`，未接入返回 `TELLO` | `TELLO` / `RMTT` |
| `wifiversion?` | 查询开源控制器 Wi-Fi 版本 | 仅开源控制器支持 | `wifivx.x.x.x` |
| `ap?` | 获取当前待连接的路由器名称和密码 | 仅开源控制器支持 | 待连接的路由器名称和密码 |
| `ssid?` | 获取飞行器当前的 SSID | 仅开源控制器支持<br>处于 STA 模式下：出厂默认的 SSID<br>处于 AP 模式下：用户自定义的 SSID 和密码 | SSID 信息 |
| `multiwifi ssid pass` | 设置开源控制器 SSID 与密码，支持连接多台设备作为路由器使用 | — | `ok` / `error` |

---

## 10. 开源控制器 → Tello 命令

对开源控制器 ESP32 进行编程，通过串口发送 `[TELLO]`（空格）+ UDP→Tello 命令，可实现与 UDP→Tello 命令描述相同的效果。

**注意：** 开源控制器 ESP32 接收的来自 TELLO 的命令均带有 `ETT`（空格）前缀及 `\r\n`（换行符）后缀。

**示例：** 开源控制器 ESP32 控制飞机起飞

- **ESP32 → Tello：** `[TELLO] takeoff`
- **Tello → ESP32：** `ETT ok\r\n`

---

## 11. UDP → 开源控制器 ESP32

> *仅出厂固件支持，恢复出厂固件方式请见用户手册。*

| 命令 | 描述 | 参数范围 | 可能的响应 |
|:---|:---|:---|:---|
| `EXT led r g b` | 点亮顶部 LED 为指定颜色 | `r, g, b`: 0~255（分别代表红、绿、蓝通道） | `led ok` / `error` |
| `EXT led br t r g b` | 顶部 LED 按照呼吸最大亮度 r、g、b 和呼吸频率 t 显示呼吸效果。从最暗变化到最亮再到最暗为一次呼吸。 | `r, g, b`: 0~255<br>`t`: 0.1~2.5 Hz | `led ok` / `error` |
| `EXT led bl t a1 b1 c1 a2 b2 c2` | 顶部 LED 以颜色 1 (r1,g1,b1) 和颜色 2 (r2,g2,b2) 按照闪烁频率 t 交替闪烁 | `a1, b1, c1, a2, b2, c2`: 0~255<br>`t`: 0.1~10 Hz | `led ok` / `error` |
| `EXT mled g xxxx` | 点亮点阵屏为指定图案 | `xxxx`：（*注 3） | `mled ok` / `error` |
| `EXT mled l/r/u/d r/b/p t xxxx` | 点阵屏显示字符串移动效果 | `l/r/u/d`：移动方向为左/右/上/下<br>`r/b/p`：字符串显示颜色（红/蓝/紫）<<br>`t`: 0.1~2.5 Hz，代表移动一帧图像频率<br>`xxxx`：需要显示的字符串，最大长度需小于 70 | `mled ok` / `error` |
| `EXT mled l/r/u/d g t xxxx` | 点阵屏显示图像移动 | `l/r/u/d`：移动方向为左/右/上/下<br>`r/b/p`：字符串显示颜色<br>`t`: 0.1~2.5 Hz，代表一帧图像移动频率<br>`xxxx`：（*注 3） | `mled ok` / `error` |
| `EXT mled s r/b/p xxxx` | 显示静态 ASCII 字符或预设图案 | `r/b/p`：字符串显示颜色<br>`xxxxx`：仅可取值 `heart` 或 ASCII 码字符 | `mled ok` / `error` |
| `EXT mled sg xxxx` | 设置点阵开机动画，设置后每次开机后会显示用户设置的图案 | `xxxx`：（*注 3） | `mled ok` / `error` |
| `EXT mled sc` | 清除设置的开机动画 | — | `mled ok` / `error` |
| `EXT mled sl n` | 设置点阵亮度 | `n`: 0~255 | `mled ok` / `error` |
| `EXT tof?` | 读取 ToF 值 | 单位为 mm，超过探测范围返回 8192 | ToF 数值 |
| `EXT version?` | 读取开源控制器 ESP32 固件版本 | — | `esp32vx.x.x.x` |

---

## 12. Tello 状态详解

### 12.1 数据格式

**数据类型：** 字符串

**格式示例：**
```text
mid:%d;x:%d;y:%d;z:%d;mpry:%d,%d,%d;pitch:%d;roll:%d;yaw:%d;vgx:%d;vgy:%d;vgz:%d;templ:%d;temph:%d;tof:%d;h:%d;bat:%d;baro:%f;time:%d;agx:%d;agy:%d;agz:%d;\r\n
```

### 12.2 字段说明

| 字段    | 说明                                               | 特殊返回值                                                   |
| :------ | :------------------------------------------------- | :----------------------------------------------------------- |
| `mid`   | 检测到的挑战卡 ID                                  | 未打开挑战卡探测功能返回 `-2`<br>打开探测功能但未检测到挑战卡返回 `-1` |
| `x`     | 飞行器相对于检测到的挑战卡的 X 轴坐标，单位：厘米  | 未打开挑战卡探测功能返回 `-200`<br>打开探测功能但未检测到挑战卡返回 `-100` |
| `y`     | 飞行器相对于检测到的挑战卡的 Y 轴坐标，单位：厘米  | 未打开挑战卡探测功能返回 `-200`<br>打开探测功能但未检测到挑战卡返回 `-100` |
| `z`     | 飞行器相对于检测到的挑战卡的 Z 轴坐标，单位：厘米  | 未打开挑战卡探测功能返回 `-200`<br>打开探测功能但未检测到挑战卡返回 `-100` |
| `mpry`  | 飞行器在挑战卡中的俯仰、偏航及横滚角度，单位：度数 | 若未检测到挑战卡，返回 `0`                                   |
| `pitch` | 俯仰角度，单位：度数                               | —                                                            |
| `roll`  | 横滚角度，单位：度数                               | —                                                            |
| `yaw`   | 偏航角度，单位：度数                               | —                                                            |
| `vgx`   | X 轴速度，单位：分米每秒                           | —                                                            |
| `vgy`   | Y 轴速度，单位：分米每秒                           | —                                                            |
| `vgz`   | Z 轴速度，单位：分米每秒                           | —                                                            |
| `templ` | 主板最低温度，单位：摄氏度                         | —                                                            |
| `temph` | 主板最高温度，单位：摄氏度                         | —                                                            |
| `tof`   | ToF 距离，单位：厘米                               | —                                                            |
| `h`     | 相对起飞点高度，单位：厘米                         | —                                                            |
| `bat`   | 当前电量百分比，`%`                                | —                                                            |
| `baro`  | 气压计测量高度，单位：米                           | —                                                            |
| `time`  | 电机运转时间，单位：秒                             | —                                                            |
| `agx`   | X 轴加速度，单位：厘米每二次方秒                   | —                                                            |
| `agy`   | Y 轴加速度，单位：厘米每二次方秒                   | —                                                            |
| `agz`   | Z 轴加速度，单位：厘米每二次方秒                   | —                                                            |

---

## 13. Mid 命令限制

涉及到 `mid` 的命令需要辅助挑战卡使用，且 `go`、`curve`、`jump` 指令只有下视摄像头识别才可以使用。

包括以下命令：

- `mon`
- `moff`
- `mdirection x`
- `go x y z speed mid`
- `curve x1 y1 z1 x2 y2 z2 speed mid`
- `jump x y z speed yaw mid1 mid2`

> 挑战卡使用说明请通过官方页面下载。

---

## 14. 附录：注释说明

### 注 1：起桨模式
起桨模式为 TT 新增的螺旋桨低速旋转状态。进入起桨模式表示 TT 已经准备好起飞，同时也可以给 TT 散热避免温度过高导致飞行器关机。飞机静止待机状态下才允许发送 `motoron` 指令进入起桨模式，飞机起飞后自动退出起桨模式。

### 注 2：挑战卡 ID 说明
- `m1` ~ `m8`：对应挑战卡上的挑战卡 ID
- `m-1`：Tello 内部算法最快识别到的挑战卡
- `m-2`：距离 Tello 最近的挑战卡

### 注 3：点阵屏图案格式
`xxxx` 代表仅由 `r`、`b`、`p`、`0` 组成的字符串，`r`、`b`、`p`、`0` 分别代表红、蓝、紫、灭。字符串长度最大为 64。

**示例：** `rrrbb0ppp` 代表第 0~3 个灯亮红色，4~5 亮蓝色，6 不亮，7~9 显示紫色。若长度不足 64，不足的部分灯珠熄灭。

---

> **声明：** 内容如有更新，恕不另行通知。您可以在官方网站查询最新版本《使用说明》。
>
> **Copyright ©2021 睿炽科技版权所有**
